{
  "name": "marko-stream-search-template",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rimraf dist && cross-env NODE_ENV=production vite build --app",
    "dev": "vite",
    "preview": "npm run build && npm start",
    "start": "cross-env NODE_ENV=production node --enable-source-maps index.ts"
  },
  "dependencies": {
    "marko": "^6.0.0"
  },
  "devDependencies": {
    "@marko/vite": "^6.1.0",
    "@types/express": "^5.0.6",
    "cross-env": "^10.1.0",
    "express": "^5.2.1",
    "rimraf": "^6.1.3",
    "vite": "^8.1.3"
  }
}
