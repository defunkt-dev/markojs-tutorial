import App from "./app.marko";

// TODO: mount the App template into the #app element.
// App.mount(input, node) renders the template into a DOM node — pass
// { name: "..." } as the input and document.getElementById("app") as the node.
