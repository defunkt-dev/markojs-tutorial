<div style="padding:1rem 1.25rem;border-radius:8px;background:#fff0f5;border:1px solid #f3c2d6;color:#8a1f4b;font-family:system-ui,sans-serif">
  <strong>Heads up</strong> — this notice is rendered by the remote app on port 3001, then
  embedded into the host page on port 3000. The host never touches its markup.
</div>
