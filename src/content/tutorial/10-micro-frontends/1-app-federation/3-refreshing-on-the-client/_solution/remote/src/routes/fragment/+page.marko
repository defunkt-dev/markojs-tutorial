<div style="padding:1rem 1.25rem;border-radius:8px;background:#fff0f5;border:1px solid #f3c2d6;color:#8a1f4b;font-family:system-ui,sans-serif">
  <strong>Heads up</strong> — this notice is rendered by the remote app.
  <div style="margin-top:.4rem;font-size:.85em;opacity:.8">rendered at ${new Date().toLocaleTimeString()}</div>
</div>
