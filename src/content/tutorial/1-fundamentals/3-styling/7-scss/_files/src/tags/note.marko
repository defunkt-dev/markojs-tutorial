<style>
  .note {
    border-left: 4px solid #7c3aed;
    background: #f5f3ff;
    padding: 12px 16px;
    border-radius: 6px;
    max-width: 320px;
  }
  .note .title {
    color: #7c3aed;
    font-weight: bold;
    margin: 0 0 4px;
  }
  .note .tag {
    color: #7c3aed;
    font-size: 0.85rem;
  }
  .note:hover {
    box-shadow: 0 2px 8px #7c3aed;
  }
</style>

<div class="note">
  <p class="title">${input.title}</p>
  <p>${input.body}</p>
  <p class="tag">${input.tag}</p>
</div>
