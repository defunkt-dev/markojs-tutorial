<style>
  .toast {
    border-left: 6px solid ${input.tone};
    background: #f8fafc;
    padding: 10px 14px;
    margin-bottom: 8px;
    max-width: 320px;
  }
  .toast strong {
    color: ${input.tone};
  }
</style>

<div class="toast">
  <strong>${input.title}</strong> ${input.message}
</div>
