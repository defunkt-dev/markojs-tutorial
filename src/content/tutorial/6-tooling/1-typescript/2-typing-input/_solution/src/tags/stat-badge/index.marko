export interface Input {
  label: string;
  value: number;
  hint?: string;
}

<div class="badge">
  <strong>${input.value}</strong>
  <span>${input.label}</span>
  <if=input.hint>
    <small>${input.hint}</small>
  </if>
</div>
