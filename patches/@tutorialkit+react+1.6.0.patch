diff --git a/node_modules/@tutorialkit/react/dist/Panels/EditorPanel.js b/node_modules/@tutorialkit/react/dist/Panels/EditorPanel.js
index 109e4eb..a58c726 100644
--- a/node_modules/@tutorialkit/react/dist/Panels/EditorPanel.js
+++ b/node_modules/@tutorialkit/react/dist/Panels/EditorPanel.js
@@ -61,6 +61,9 @@ function getFileIcon(fileName) {
         case 'json': {
             return 'i-languages-json?mask';
         }
+        case 'marko': {
+            return 'i-languages-marko';
+        }
         case 'gif':
         case 'jpg':
         case 'jpeg':
diff --git a/node_modules/@tutorialkit/react/dist/Panels/PreviewPanel.js b/node_modules/@tutorialkit/react/dist/Panels/PreviewPanel.js
index 8fe0723..0263d95 100644
--- a/node_modules/@tutorialkit/react/dist/Panels/PreviewPanel.js
+++ b/node_modules/@tutorialkit/react/dist/Panels/PreviewPanel.js
@@ -54,7 +54,7 @@ export const PreviewPanel = memo(forwardRef(({ showToggleTerminal, toggleTermina
         }
     }
     if (!hasPreviews) {
-        return (_jsxs("div", { className: "panel-container transition-theme bg-tk-elements-panel-backgroundColor text-tk-elements-panel-textColor", children: [_jsxs("div", { className: "panel-header border-b border-tk-elements-app-borderColor justify-between", children: [_jsxs("div", { className: "panel-title", children: [_jsx("div", { className: "panel-icon i-ph-lightning-duotone" }), _jsx("span", { className: "text-sm", children: i18n.prepareEnvironmentTitleText })] }), showToggleTerminal && (_jsxs("button", { className: "panel-button px-2 py-0.5 -mr-1 -my-1", title: "Toggle Terminal", onClick: () => toggleTerminal?.(), children: [_jsx("span", { className: "panel-button-icon i-ph-terminal-window-duotone" }), _jsx("span", { className: "text-sm", children: i18n.toggleTerminalButtonText })] }))] }), _jsx(BootScreen, { tutorialStore: tutorialStore })] }));
+        return (_jsxs("div", { className: "panel-container transition-theme bg-tk-elements-panel-backgroundColor text-tk-elements-panel-textColor", children: [_jsxs("div", { className: "panel-header border-b border-tk-elements-app-borderColor justify-between", children: [_jsxs("div", { className: "panel-title", children: [_jsx("div", { className: "panel-icon i-ph-lightning-duotone" }), _jsx("span", { className: "text-sm", children: i18n.prepareEnvironmentTitleText })] }), showToggleTerminal && (_jsxs("button", { className: "panel-button px-2 py-0.5 -mr-1 -my-1", title: "Toggle Terminal", onClick: () => toggleTerminal?.(), children: [_jsx("span", { className: "panel-button-icon i-languages-terminal-toggle" }), _jsx("span", { className: "text-sm", children: i18n.toggleTerminalButtonText })] }))] }), _jsx(BootScreen, { tutorialStore: tutorialStore })] }));
     }
     const previews = expectedPreviews.filter((preview) => preview.ready);
     const defaultSize = 100 / previews.length;
@@ -90,7 +90,7 @@ function Preview({ preview, iframe, previewCount, first, last, toggleTerminal, i
     }
     return (_jsxs("div", { className: "panel-container", children: [_jsxs("div", { className: classNames('panel-header border-b border-tk-elements-app-borderColor justify-between', {
                     'border-l border-tk-elements-app-borderColor': !first,
-                }), children: [_jsxs("div", { className: "panel-title", children: [_jsx("button", { onClick: reload, title: i18n.reloadPreviewTitle, className: "panel-button rounded-full p-1.5 -my-1.5 -ml-2", children: _jsx("div", { className: "panel-icon i-ph-arrow-clockwise" }) }), _jsx("span", { className: "text-sm truncate", children: previewTitle(preview, previewCount, i18n) })] }), last && (_jsxs("button", { className: "panel-button px-2 py-0.5 -mr-1 -my-1", title: "Toggle Terminal", onClick: () => toggleTerminal?.(), children: [_jsx("div", { className: "panel-button-icon i-ph-terminal-window-duotone" }), _jsx("span", { className: "text-sm", children: i18n.toggleTerminalButtonText })] }))] }), _jsx("div", { ref: previewContainerRef, className: classNames('h-full w-full flex justify-center items-center', {
+                }), children: [_jsxs("div", { className: "panel-title", children: [_jsx("button", { onClick: reload, title: i18n.reloadPreviewTitle, className: "panel-button rounded-full p-1.5 -my-1.5 -ml-2", children: _jsx("div", { className: "panel-icon i-ph-arrow-clockwise" }) }), _jsx("span", { className: "text-sm truncate", children: previewTitle(preview, previewCount, i18n) })] }), last && (_jsxs("button", { className: "panel-button px-2 py-0.5 -mr-1 -my-1", title: "Toggle Terminal", onClick: () => toggleTerminal?.(), children: [_jsx("div", { className: "panel-button-icon i-languages-terminal-toggle" }), _jsx("span", { className: "text-sm", children: i18n.toggleTerminalButtonText })] }))] }), _jsx("div", { ref: previewContainerRef, className: classNames('h-full w-full flex justify-center items-center', {
                     'border-l border-tk-elements-previews-borderColor': !first,
                 }) })] }));
 }
diff --git a/node_modules/@tutorialkit/react/dist/Panels/TerminalPanel.js b/node_modules/@tutorialkit/react/dist/Panels/TerminalPanel.js
index 777bad7..62e6ad5 100644
--- a/node_modules/@tutorialkit/react/dist/Panels/TerminalPanel.js
+++ b/node_modules/@tutorialkit/react/dist/Panels/TerminalPanel.js
@@ -16,8 +16,17 @@ export function TerminalPanel({ theme, tutorialStore }) {
     useEffect(() => {
         setDomLoaded(true);
     }, []);
+    const panelIdsRef = useRef('');
     useEffect(() => {
-        setTabIndex(terminalConfig.activePanel);
+        // Every lesson navigation produces a new TerminalConfig object. Resetting the
+        // active tab each time flips the interactive terminal to `display: none` and back,
+        // which churns its layout (and used to re-broadcast stale terminal dimensions).
+        // Only honour `activePanel` when the set of panels actually changes.
+        const panelIds = terminalConfig.panels.map((panel) => panel.id).join(',');
+        if (panelIds !== panelIdsRef.current) {
+            panelIdsRef.current = panelIds;
+            setTabIndex(terminalConfig.activePanel);
+        }
     }, [terminalConfig]);
     useEffect(() => {
         return tutorialStore.themeRef.subscribe(() => {
@@ -40,6 +49,6 @@ export function TerminalPanel({ theme, tutorialStore }) {
                         }) }) }) }), _jsx("div", { className: "h-full overflow-hidden", children: domLoaded && (_jsx(Suspense, { children: terminalConfig.panels.map(({ id, type }, index) => (_jsx(Terminal, { role: "tabpanel", id: `tk-terminal-tapbanel-${index}`, "aria-labelledby": `tk-terminal-tab-${index}`, className: tabIndex !== index ? 'hidden h-full' : 'h-full', theme: theme, readonly: type === 'output', ref: (ref) => (terminalRefs.current[index] = ref), onTerminalReady: (terminal) => {
                             tutorialStore.attachTerminal(id, terminal);
                         }, onTerminalResize: (cols, rows) => {
-                            tutorialStore.onTerminalResize(cols, rows);
+                            tutorialStore.onTerminalResize(cols, rows, id);
                         } }, id))) })) })] }));
 }
diff --git a/node_modules/@tutorialkit/react/dist/core/CodeMirrorEditor/languages.js b/node_modules/@tutorialkit/react/dist/core/CodeMirrorEditor/languages.js
index ed5edcb..69bdc7a 100644
--- a/node_modules/@tutorialkit/react/dist/core/CodeMirrorEditor/languages.js
+++ b/node_modules/@tutorialkit/react/dist/core/CodeMirrorEditor/languages.js
@@ -91,6 +91,13 @@ export const supportedLanguages = [
             return import('@replit/codemirror-lang-svelte').then((module) => module.svelte());
         },
     }),
+    LanguageDescription.of({
+        name: 'Marko',
+        extensions: ['marko'],
+        async load() {
+            return import('codemirror-lang-marko').then((module) => module.marko());
+        },
+    }),
 ];
 export async function getLanguage(fileName) {
     const languageDescription = LanguageDescription.matchFilename(supportedLanguages, fileName);
diff --git a/node_modules/@tutorialkit/react/dist/core/FileTree.js b/node_modules/@tutorialkit/react/dist/core/FileTree.js
index 900cbd7..8873c8d 100644
--- a/node_modules/@tutorialkit/react/dist/core/FileTree.js
+++ b/node_modules/@tutorialkit/react/dist/core/FileTree.js
@@ -183,6 +183,7 @@ const extensionsToIcons = new Map([
     ['css', 'i-ph-file-css-duotone'],
     ['md', 'i-ph-file-md-duotone'],
     ['vue', 'i-ph-file-vue-duotone'],
+    ['marko', 'i-languages-marko'],
     ['gif', 'i-ph-file-image-duotone'],
     ['jpg', 'i-ph-file-image-duotone'],
     ['jpeg', 'i-ph-file-image-duotone'],
diff --git a/node_modules/@tutorialkit/react/dist/core/Terminal/index.js b/node_modules/@tutorialkit/react/dist/core/Terminal/index.js
index 91468f3..fca27b5 100644
--- a/node_modules/@tutorialkit/react/dist/core/Terminal/index.js
+++ b/node_modules/@tutorialkit/react/dist/core/Terminal/index.js
@@ -6,11 +6,15 @@ import '@xterm/xterm/css/xterm.css';
 import { forwardRef, useEffect, useImperativeHandle, useRef } from 'react';
 import '../../styles/terminal.css';
 import { getTerminalTheme } from './theme.js';
+let __tkDiagSeq = 0;
+const __tkDiag = (...args) => console.log('%c[tk-diag]', 'color:#c026d3', ...args);
 export const Terminal = forwardRef(({ theme, readonly = true, onTerminalReady, onTerminalResize, ...props }, ref) => {
     const divRef = useRef(null);
     const terminalRef = useRef();
     useEffect(() => {
         const element = divRef.current;
+        const __id = `${readonly ? 'output' : 'interactive'}#${++__tkDiagSeq}`;
+        __tkDiag('xterm MOUNT', __id, { rendered: !!element.offsetParent });
         const fitAddon = new FitAddon();
         const webLinksAddon = new WebLinksAddon();
         const terminal = new XTerm({
@@ -25,14 +29,42 @@ export const Terminal = forwardRef(({ theme, readonly = true, onTerminalReady, o
         terminal.loadAddon(fitAddon);
         terminal.loadAddon(webLinksAddon);
         terminal.open(element);
-        fitAddon.fit();
+        // A panel whose tab is inactive is rendered with `display: none`, which gives it
+        // no box: fitting it then measures nothing and reports whatever stale dimensions
+        // the terminal happens to hold. Those get broadcast to EVERY process (see
+        // TerminalStore.onTerminalResize), resizing the live shell's PTY behind the
+        // user's back and leaving the visible terminal redrawn at the wrong geometry.
+        // Only ever fit/report while the element is actually laid out.
+        // A terminal is only measurable when it is laid out AND big enough to hold a
+        // couple of rows. A hidden tab (display:none) has no box at all; a COLLAPSED
+        // terminal panel still has one, but fitting it proposes the addon's minimum
+        // geometry (1 row) which would then be pushed onto the live shell's PTY,
+        // destroying its screen. Neither is a real resize: ignore both.
+        const MIN_MEASURABLE_PX = 40;
+        const isRendered = () => {
+            if (!element.offsetParent && element.getClientRects().length === 0) {
+                return false;
+            }
+            return element.offsetHeight >= MIN_MEASURABLE_PX && element.offsetWidth >= MIN_MEASURABLE_PX;
+        };
+        if (isRendered()) {
+            fitAddon.fit();
+        }
         const resizeObserver = new ResizeObserver(() => {
+            if (!isRendered()) {
+                __tkDiag('resize SKIPPED (hidden)', __id, `${terminal.cols}x${terminal.rows}`);
+                return;
+            }
+            const before = `${terminal.cols}x${terminal.rows}`;
             fitAddon.fit();
+            __tkDiag('resize', __id, `${before} -> ${terminal.cols}x${terminal.rows}`);
             onTerminalResize?.(terminal.cols, terminal.rows);
         });
         resizeObserver.observe(element);
+        __tkDiag('xterm READY', __id, `${terminal.cols}x${terminal.rows}`);
         onTerminalReady?.(terminal);
         return () => {
+            __tkDiag('xterm DISPOSE', __id);
             resizeObserver.disconnect();
             terminal.dispose();
         };
